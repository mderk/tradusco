Output format:

- Return only one JSON object with a `translations` array.
- Include one object per requested language: `{"language": "<code>", "translations": ["..."]}`.
- Use each requested language code exactly.
- Each language's translations must stay in the same order as the input phrases.
- Do not include code fences or any extra text.
