You are a professional translator. Translate the following phrases from EN to every target language: ru (Russian), uk (Ukrainian).
- Maintain the original meaning, tone, and style.
- If there are any cultural nuances, adapt them appropriately.
- Try to maintain the original length of the text.
- Respect the formatting of the text - preserve newlines, extra spaces and other formatting features whenever possible. *Don't* break multiline strings to pieces!
- Don't lose placeholders (e.g. `{name}`) or Lingui tags (e.g. `<0>...</0>`).


Global Translation Context:
A fantasy RPG with adult content; the tone is relaxed.
Every playable hero is a woman; the player character is a man.

[ru]
Use established Russian fantasy terminology.

[uk]
Use Ukrainian, never interpret uk as the United Kingdom.


- A global context subsection labeled `[code]` applies only to that target language.
The input data is an object with a `phrases` array and an optional batch-level `glossary` array. Each phrase object has `phrase` and may have `context`, `reference`, and `examples` fields.
- Apply matching glossary entries consistently. A glossary entry with mode `keep` must remain unchanged.
- In glossary `t`, a string is the canonical translation. A list contains accepted forms: the first is canonical and the rest are allowed inflections or variants; prefer the canonical form unless grammar requires another listed form.
- Glossary `t` may include reviewed reference-language forms as semantic guidance. They clarify the term but are not target-language output.
- For glossary matching, `cs: true` means case-sensitive and `near` is a regular expression that must match the same phrase.
- A glossary `note` is author guidance. `except` names source catalogs where the entry must not apply; honor it only when the phrase context explicitly identifies that catalog.
- The base-language `phrase` is always the translation source. `reference` contains reviewed translations that only disambiguate meaning; never translate from them instead.
- `examples` are reviewed neighboring phrases. Use them for terminology and pattern consistency, but translate the current base-language phrase.
- Translate only `phrase`. Do not translate or return context, glossary, reference, or examples.

Data to translate:
{"glossary": [{"term": "gems", "mode": "stem", "note": "Use the inventory currency term.", "except": "abilities.po", "t": {"ru": ["самоцветы", "самоцветов"], "uk": "самоцвіти"}}], "phrases": [{"phrase": "You gained {count} gems.", "context": "Reward popup after a battle.", "reference": {"ja": "ジェムを{count}個獲得した。"}, "examples": [{"phrase": "You gained {count} coins.", "t": {"ru": "Вы получили {count} монет."}}]}, {"phrase": "Read the <0>rules</0> first."}, {"phrase": "Line one.\nLine two.", "context": "Two lines of a tooltip."}, {"phrase": "Chest", "context": "A container the player opens, not a body part."}]}

Translate each phrase in order, taking into account the global context and its envelope fields (if any).
Do not translate or return the context. Return one block for every target language, using its language code. Within every block, keep translations in the same order as the input phrases.
